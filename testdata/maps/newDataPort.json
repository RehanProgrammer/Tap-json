
{
    "__rootArray": [
        "{{__rootArray}}",
        {
          "Fields":[
              "{{record.Fields}}",
              {
                  "TapName": "{{TargetName}}"
                   
              }
          ]
        }
    ]
}